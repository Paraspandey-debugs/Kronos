# `api/v1/health.routes.ts`

## Overview
Defines the routing sub-tree for application health checks.

## Components
- Mounts a basic `GET /` router that connects directly to the `healthCheck` controller function.
